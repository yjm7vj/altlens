# Backend origin for the Vite dev proxy. Change this if port 8000 is taken.
VITE_PROXY_TARGET=http://127.0.0.1:8000

# Absolute API origin for a deployed build. Leave unset in development so the
# dev proxy handles /api.
# VITE_API_BASE_URL=https://altlens-api.onrender.com
